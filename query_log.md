```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
INSERT INTO PlayTennisDB VALUES (
            TestD15, 
            Sunny, 
            Hot, 
            High, 
            Weak, 
            Yes);
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
UPDATE PlayTennisDB SET 
        day=TestD15, 
        outlook=Sunny,
        temp=Hot, 
        humidity=High, 
        wind=Weak, 
        play=No 
        WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

```sql
DELETE FROM PlayTennisDB WHERE id=15;
```

```sql
SELECT * FROM PlayTennisDB;
```

